{
  "actorOutputSchemaVersion": 1,
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "template": "Title: {{title}}"
    },
    "url": {
      "type": "string",
      "template": "URL: {{url}}"
    },
    "price": {
      "type": "string",
      "template": "Price: {{price}}"
    },
    "manufacturer": {
      "type": "string",
      "template": "Manufacturer: {{manufacturer}}"
    },
    "model": {
      "type": "string",
      "template": "Model: {{model}}"
    },
    "year": {
      "type": "string",
      "template": "Year: {{year}}"
    },
    "hours": {
      "type": "string",
      "template": "Hours: {{hours}}"
    },
    "horsepower": {
      "type": "string",
      "template": "HP: {{horsepower}}"
    },
    "condition": {
      "type": "string",
      "template": "Condition: {{condition}}"
    },
    "location": {
      "type": "string",
      "template": "Location: {{location}}"
    },
    "seller": {
      "type": "string",
      "template": "Seller: {{seller}}"
    },
    "stockNumber": {
      "type": "string",
      "template": "Stock: {{stockNumber}}"
    },
    "category": {
      "type": "string",
      "template": "Category: {{category}}"
    },
    "scrapedAt": {
      "type": "string",
      "template": "Scraped: {{scrapedAt}}"
    }
  }
}
